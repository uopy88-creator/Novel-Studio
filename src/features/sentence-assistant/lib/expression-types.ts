/**
 * =============================================================================
 * Expression — 타입
 * -----------------------------------------------------------------------------
 * 표현 탭은 유의어 · 관용 표현만 다룬다.
 * =============================================================================
 */

export interface ExpressionLookupResult {
  query: string;
  /** 가나다순 유의어 */
  synonyms: string[];
  /** 가나다순 관용 표현 */
  idioms: string[];
}

export const EXPRESSION_NOT_FOUND_MESSAGE = "관련 표현을 찾을 수 없습니다.";
