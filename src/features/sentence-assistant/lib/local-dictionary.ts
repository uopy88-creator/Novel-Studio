/**
 * =============================================================================
 * 로컬 폴백 사전 (개발·API 키 없을 때)
 * -----------------------------------------------------------------------------
 * 표준국어대사전 API 가 없거나 실패할 때 최소 뜻만 제공한다.
 * 프로덕션에서는 STDICT_API_KEY 로 국어원 API를 우선 사용한다.
 * =============================================================================
 */

const LOCAL_DEFINITIONS: Record<string, string> = {
  슬프다: "마음이 괴롭거나 서운한 감정을 느끼다.",
  기쁘다: "욕구가 충족되어 흐뭇하고 만족스럽다.",
  화나다: "성이 나서 마음이 몹시 언짢다.",
  사랑: "어떤 사람이나 존재를 몹시 아끼고 귀중히 여기는 마음.",
  희망: "앞으로 잘될 수 있는 가능성을 기대함. 또는 그런 가능성.",
  어둠: "빛이 없어 캄캄한 상태.",
  빛: "눈의 감각을 자극하여 사물을 볼 수 있게 하는 물질의 한 요소.",
  길: "사람이나 차량 따위가 지나다닐 수 있게 뚫어 놓은 공간.",
  집: "사람이나 동물이 살기 위해 지은 건물.",
  사람: "생각을 하고 언어를 사용하며 도구를 만들어 쓰고 사회를 이루어 사는 동물.",
};

export function lookupLocalDefinition(query: string): string | null {
  const key = query.trim();
  if (!key) return null;
  return LOCAL_DEFINITIONS[key] ?? LOCAL_DEFINITIONS[key.replace(/다$/, "다")] ?? null;
}
