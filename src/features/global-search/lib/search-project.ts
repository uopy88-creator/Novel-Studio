/**
 * =============================================================================
 * searchProject — SearchService 하위 호환 래퍼
 * =============================================================================
 */

export {
  searchProject,
  flattenSearchGroups,
  searchService,
  SearchService,
  keywordSearchProvider,
} from "@/features/global-search/lib/search-service";
